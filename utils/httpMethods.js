/**
 * Object that represents all the http methods as a string
 * @type {{DELETE: string, POST: string, GET: string, OPTIONS: string, PUT: string}}
 */
const httpMethods = {
    GET: 'GET',
    POST: 'POST',
    PUT: 'PUT',
    OPTIONS: 'OPTIONS',
    DELETE: 'DELETE'
}

module.exports = httpMethods;